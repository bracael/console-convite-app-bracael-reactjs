import { Story, Meta } from '@storybook/react/types-6-0'

import Keyboard from '.'

export default {
  title: 'Keyboard',
  component: Keyboard
} as Meta

export const Default: Story = () => <Keyboard />
